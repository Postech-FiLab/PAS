import 'package:flutter/material.dart';

class DataPreprocess extends StatelessWidget {
  const DataPreprocess({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("dadta");
  }
}
