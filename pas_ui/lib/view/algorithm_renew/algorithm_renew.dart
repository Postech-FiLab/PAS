import 'package:flutter/material.dart';

class AlgorithmRenew extends StatelessWidget {
  const AlgorithmRenew({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("algo");
  }
}
