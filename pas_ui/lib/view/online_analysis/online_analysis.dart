import 'package:flutter/material.dart';

class OnlineAnalysis extends StatelessWidget {
  const OnlineAnalysis({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("online");
  }
}
